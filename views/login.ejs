<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Energy Ease</title>

  <style>
    body, html {
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      height: 100%;
      background: url('/images/labtekback.png') no-repeat center center/cover;
    }

    .login-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: calc(100% - 70px);
      text-align: center;
      padding: 20px;
    }

    .login-title {
      font-size: 28px;
      font-weight: bold;
      color: white;
      text-shadow: 1px 1px 3px black;
    }

    .login-subtitle {
      font-size: 16px;
      color: white;
      margin-bottom: 30px;
      text-shadow: 1px 1px 2px black;
    }

    .form-box {
      background-color: rgba(255, 255, 255, 0.85);
      padding: 30px 25px;
      border-radius: 15px;
      width: 300px;
      box-shadow: 0 0 10px rgba(0,0,0,0.2);
    }

    .form-box input {
      width: 100%;
      padding: 10px;
      margin: 10px 0;
      border: none;
      border-radius: 25px;
      background-color: white;
      font-size: 14px;
    }

    .form-box button {
      width: 100%;
      padding: 12px;
      margin-top: 15px;
      background-color: green;
      color: white;
      font-size: 16px;
      font-weight: bold;
      border: none;
      border-radius: 25px;
      cursor: pointer;
    }

    .form-box .register-link {
      margin-top: 15px;
      font-size: 14px;
    }

    .form-box .register-link a {
      color: #1a4dd7;
      text-decoration: none;
      font-weight: bold;
    }

    .form-box .register-link a:hover {
      text-decoration: underline;
    }

    .error-message {
      color: red;
      font-size: 14px;
    }
  </style>
</head>
<body>

  <%- include('partials/headerGuest') %>

  <div class="login-container">
    <div class="login-title">Energy Management System<br>Based by IoT</div>
    <div class="login-subtitle">Sistem Manajemen Energi berbasis IoT</div>

    <div class="form-box">
      <% if (error) { %>
        <div class="error-message"><%= error %></div>
      <% } %>
      <p>Masuk dengan akun anda</p>
      <form action="/login" method="POST">
        <input type="email" name="email" placeholder="Email" required />
        <input type="password" name="password" placeholder="Password" required />
        <button type="submit">Login</button>
      </form>

      <div class="register-link">
        Tidak punya akun? daftar <a href="/register">disini</a>
      </div>
    </div>
  </div>

</body>
</html>
