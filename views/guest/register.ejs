<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Daftar Akun - Energy Ease</title>
    <style>
      body {
        margin: 0;
        font-family: sans-serif;
        background-color: #f0f2f5;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        color: #333;
      }

      .page-title {
        font-size: 48px;
        font-weight: bold;
        color: #2c3e50;
        margin-bottom: 30px;
      }

      .back-link {
        position: absolute;
        top: 20px;
        left: 20px;
        text-decoration: none;
        font-weight: bold;
        color: #2c3e50;
        background-color: #ffffff;
        padding: 8px 16px;
        border-radius: 8px;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
      }

      .back-link:hover {
        background-color: #3498db;
        color: #fff;
      }

      .register-container {
        width: 100%;
        max-width: 400px;
        padding: 30px;
        background-color: #ffffff;
        border-radius: 10px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        text-align: center;
      }

      .register-title {
        font-size: 28px;
        font-weight: bold;
        color: #2c3e50;
        margin-bottom: 25px;
      }

      .error-message {
        color: #e74c3c;
        background-color: #fdeded;
        border: 1px solid #e74c3c;
        border-radius: 5px;
        padding: 10px;
        margin-bottom: 20px;
      }

      .form-box {
        width: 100%;
        max-width: 380px;
        text-align: left;
      }

      .form-box p {
        font-size: 16px;
        color: #555;
        margin-bottom: 20px;
        text-align: center;
      }

      .form-box label {
        display: block;
        font-size: 14px;
        color: #333;
        font-weight: bold;
        margin-top: 15px;
        margin-bottom: 5px;
      }

      .form-box input {
        width: 100%;
        padding: 12px;
        font-size: 16px;
        border: 1px solid #ccc;
        border-radius: 5px;
        box-sizing: border-box;
      }

      .form-box input:focus {
        border-color: #007bff;
        outline: none;
        box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
      }

      .form-box button {
        width: 100%;
        padding: 12px;
        font-size: 18px;
        font-weight: bold;
        color: #fff;
        background-color: #3498db;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease;
        margin-top: 20px;
      }

      .form-box button:hover {
        background-color: #2980b9;
      }

      .login-link {
        margin-top: 20px;
        font-size: 14px;
        color: #777;
        text-align: center;
      }

      .login-link a {
        color: #3498db;
        font-weight: bold;
        text-decoration: none;
      }

      .login-link a:hover {
        text-decoration: underline;
      }

      @media (max-width: 768px) {
        .page-title {
          font-size: 32px;
        }

        .register-container {
          width: 90%;
          padding: 20px;
        }

        .form-box input,
        .form-box button {
          font-size: 14px;
          padding: 10px;
        }
      }

      @media (max-width: 480px) {
        .page-title {
          font-size: 26px;
        }

        .register-container {
          padding: 15px;
        }

        .form-box label {
          font-size: 13px;
        }
      }
    </style>
  </head>
  <body>
    <!-- 🔧 gunakan baseUrl agar tetap bisa diakses melalui /energyease/... -->
    <a href="<%= baseUrl %>/" class="back-link">&larr; Kembali ke Beranda</a>

    <h1 class="page-title">Daftar Akun</h1>

    <div class="register-container">
      <div class="register-title">Energy Ease</div>

      <!-- Gunakan baseUrl di action -->
      <form class="form-box" method="POST" action="<%= baseUrl %>/register">
        <% if (error) { %>
        <div class="error-message">
          <% if (error === 'email_exists') { %> Email sudah terdaftar. <% } else if (error === 'username_exists') { %> Nama pengguna sudah digunakan. <% } %>
        </div>
        <% } %>

        <p>Daftar akun untuk memulai</p>

        <label for="username">Nama pengguna</label>
        <input name="username" id="username" required />

        <label for="email">Email</label>
        <input name="email" id="email" type="email" required />

        <label for="password">Password</label>
        <input name="password" id="password" type="password" required />

        <button type="submit">Daftar</button>

        <div class="login-link">
          Sudah punya akun?
          <a href="<%= baseUrl %>/login">Masuk</a>
        </div>
      </form>
    </div>
  </body>
</html>
